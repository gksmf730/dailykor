{
  "name": "dailykor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sass": "sass --watch src/scss:src/css",
    "build": "sass src/scss:src/css --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
